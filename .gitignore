# Dependencies
node_modules/

# Local database file (production uses Turso)
data/

# Secrets — never commit these. .env.local holds live Turso credentials.
.env
.env.*
!.env.example

# Vercel CLI project link
.vercel

# Business docs — never belongs in a public repo, unlike everything else
# in docs/. Kept in the working tree for convenience, gitignored on purpose.
docs/MONETIZATION-ideas.md

# OS / editor noise
.DS_Store
Thumbs.db
*.log
